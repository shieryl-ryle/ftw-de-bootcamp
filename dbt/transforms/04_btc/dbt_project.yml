name: "ex_03_meteo"
version: "1.0"
config-version: 2

profile: clickhouse_ftw
model-paths: ["models"]
macro-paths: ["macros"]  

models:
  ex_03_meteo:
    clean:
      +schema: clean
      +materialized: table
    mart:
      +schema: mart
      +materialized: view
