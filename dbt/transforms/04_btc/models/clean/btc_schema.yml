version: 2

models:
  - name: stg_btc__market_price
    description: Cleaned BTC market snapshots (UTC + Manila timestamps)
    columns:
      - name: coin_id
        tests: [not_null]
      - name: last_updated_utc
        tests: [not_null]
      - name: price_usd
        tests: [not_null]
