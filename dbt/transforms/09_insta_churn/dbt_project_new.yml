name: "ex_09_insta_churn"
version: "1.0"
config-version: 2

profile: clickhouse_ftw
model-paths: ["models"]
macro-paths: ["macros"]  

models:
  ex_09_insta_churn:
    clean:
      +schema: clean
      +materialized: table
    mart:
      +schema: mart  
      +materialized: view
    sandbox:
      +schema: sandbox
      +materialized: table